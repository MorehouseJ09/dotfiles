# initialize linux brew elements as needed
PATH="$HOME/.linuxbrew/bin:$PATH"
LD_LIBRARY_PATH="$HOME/.linuxbrew/lib"

# add gnupg libexec path 
PATH=/usr/lib/gnupg2:$PATH
# add in npm path. Note, on mac this is handled by homebrew
PATH=$HOME/.npm/bin:$PATH
PATH=/usr/sbin:$PATH

# tmux source file
TMUX_CONF=$HOME/.linux.tmux.conf

# export prompt - zsh-theme.zsh  doesn't play to nice with prompt, so export forcefully!
PROMPT='%(!.%{$fg_bold[white]%}.%{$fg_bold[white]%})%{$fg_bold[white]%}$(path_tail 2) $(git_prompt_info)%_$(prompt_char)%{$reset_color%} '
